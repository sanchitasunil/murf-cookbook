# ─── Voice & ASR ────────────────────────────────────────────────
MURF_API_KEY=your_murf_key_here
DEEPGRAM_API_KEY=your_deepgram_key_here

# ─── Swiggy ─────────────────────────────────────────────────────
# Optional / unused — Swiggy MCP auth is handled by mcporter via OAuth
# (run `mcporter auth swiggy-food` once). Leave blank or remove.
SWIGGY_API_KEY=your_swiggy_key_here_if_required

# ─── LLM provider switching ─────────────────────────────────────
# Pick which LLM provider the agent uses. Switch when one hits rate
# limits — no code change needed, just edit this line and restart.
#
#   gemini      → Google Gemini 2.5 Flash, direct API     (needs GEMINI_API_KEY)
#   openrouter  → Gemini 3.0 preview via OpenRouter       (needs OPENROUTER_API_KEY)
#   opencode    → Big Pickle (free) via SST OpenCode Zen  (needs OPENCODE_API_KEY)
#
LLM_PROVIDER=gemini

# Optional: pin a specific model on the active provider. If unset, brain.ts
# picks the default model for whichever provider is selected above.
# Examples:
#   LLM_MODEL=google/gemini-2.5-flash
#   LLM_MODEL=openrouter/google/gemini-3.0-preview
#   LLM_MODEL=opencode/big-pickle
#LLM_MODEL=

# ─── Provider keys (set the ones you use) ───────────────────────
GEMINI_API_KEY=your_gemini_key_here
OPENROUTER_API_KEY=your_openrouter_key_here

# opencode: SST OpenCode Zen gateway. Sign up at https://opencode.ai/auth to
# get a key, then uncomment and fill in below. Free tier uses `opencode/big-pickle`.
# OPENCODE_BASE_URL is optional — only set it if you need to point at a non-default
# Zen endpoint; leave unset to use the openclaw plugin's default.
#OPENCODE_API_KEY=your_opencode_zen_key_here
#OPENCODE_BASE_URL=https://opencode.ai/zen/v1
